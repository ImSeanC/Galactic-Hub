# Galactic-Hub

This repository now includes a static documentation explorer generator.

## Build the docs site

1. Install dependencies:
   ```bash
   npm install
   ```
2. Build the static site:
   ```bash
   npm run build
   ```

The generated site is available in the `dist/` folder and is ready for GitHub Pages deployment.
